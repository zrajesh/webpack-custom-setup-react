import React from "react";
import "./style.css"

const Button = () => {
    return (
        <div>
            <button className="button">Click Me</button>
        </div>
    );
};

export default Button;